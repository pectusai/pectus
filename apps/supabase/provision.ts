// CLI provisioning. Called by `pectus connect supabase`.
// Stub — implementation lands in PR2.
// Plan: create project via Management API, run migrations from ./migrations/,
// create first admin user, write env vars to .env.local.

export const STUB = true;
